---
- name: Verify connectivity
  hosts: linux_lab
  gather_facts: true

  tasks:
    - name: Show OS
      ansible.builtin.debug:
        msg: "{{ ansible_distribution }} {{ ansible_distribution_version }}"
